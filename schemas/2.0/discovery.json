{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://srs.semanticops.com/schema/2.0/discovery.json",
  "title": "SRS Discovery Contract (ext:discovery)",
  "description": "Defines DiscoveryQuery, TextSegment, and ConformanceScenario shapes for the ext:discovery extension (RFC-012).",
  "$defs": {
    "DiscoveryQuery": {
      "type": "object",
      "description": "A conjunction of zero or more structured filter predicates. An instance matches if and only if it satisfies all predicates whose values are specified (unspecified predicates are wildcards).",
      "additionalProperties": false,
      "properties": {
        "typeId": {
          "type": "string",
          "format": "uuid",
          "description": "Exact match on Record.typeId. Only Tier 2 Records carry typeId; Tier 0 instances do not match this predicate."
        },
        "typeNamespace": {
          "type": "string",
          "description": "Exact match on Record.typeNamespace. Independent predicate; can be combined with typeId."
        },
        "typeName": {
          "type": "string",
          "description": "Exact match on Record.typeName. Independent predicate; can be combined with typeId."
        },
        "containerId": {
          "type": "string",
          "format": "uuid",
          "description": "Instance is a member of this container per RFC-009 I-66: (1) instanceId in Container.rootInstanceIds[], OR (2) instanceId in Container.memberInstanceIds[], OR (3) reachable via transitive contains Relation traversal from any rootInstanceIds[] entry."
        },
        "tag": {
          "type": "array",
          "items": { "type": "string" },
          "description": "AND-conjunction: the instance's tags array must contain ALL of the specified values. RFC-006 vocabulary resolution is applied before comparison when a Vocabulary is declared."
        },
        "lifecycleState": {
          "type": "string",
          "description": "Exact match on Record.lifecycleState (case-sensitive). Requires ext:lifecycle. Instances without a lifecycleState field do not match."
        },
        "lifecycleStates": {
          "type": "array",
          "items": { "type": "string" },
          "description": "RFC-012 Rev 11 (srs#525) — inclusive multi-value lifecycle filter, OR semantics: matches an instance whose Record.lifecycleState equals ANY listed value. An instance with no lifecycleState does not match when this predicate is present and non-empty. Independent of the exact-match lifecycleState predicate above (do not combine the two on one query). Formerly SectionSource.type-query's own lifecycleStates axis (RFC-011 Change A, I-142); carried forward here as the SectionSource → DiscoveryQuery collapse's one query mechanism (srs#525). Requires ext:lifecycle; instances without ext:lifecycle declared MUST ignore this predicate."
        },
        "excludeLifecycleStates": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Exclusion filter (parity with the former RFC-011 type-query axis of the same name, now retired in its favor — srs#525): an instance whose Record.lifecycleState matches ANY listed value is excluded. Applied after lifecycleState/lifecycleStates. Lets an authored view declare default-hidden states (e.g. superseded, closed) while a client offers a runtime show-all override by passing an empty list. Requires ext:lifecycle; instances with no lifecycleState are never excluded by this axis."
        },
        "tier": {
          "enum": [0, 2],
          "description": "Instance tier filter. 0 = Note, 2 = Record. Tier 1 (TypedRecord) was removed (rfc-decision-53635966); the gap in numbering is retained deliberately for reference stability."
        },
        "contentMatch": {
          "type": "string",
          "description": "Free-text recall-floor predicate. The implementation MUST include every instance whose Text Projection contains at least one segment whose normalized text contains the normalized form of this string as a substring (NFC + Unicode simple lowercasing)."
        }
      }
    },
    "TextSegment": {
      "type": "object",
      "description": "A single searchable unit in the Text Projection of an instance. Normalization (NFC + Unicode simple lowercasing) is applied at match time, not at segment construction time.",
      "required": ["fieldId", "fieldName", "text"],
      "additionalProperties": false,
      "properties": {
        "fieldId": {
          "type": "string",
          "description": "UUID of the field definition for package-resolved fields; sentinel string for special segments: 'note-title', 'note-section', 'tag', 'label'."
        },
        "fieldName": {
          "type": "string",
          "description": "Field name (snake_case) for package-resolved fields; sentinel string for special segments matching fieldId sentinels; or NoteSection.name for named special segments."
        },
        "text": {
          "type": "string",
          "description": "The raw stored text value. Normalization is applied at match time."
        }
      }
    },
    "ConformanceScenario": {
      "type": "object",
      "description": "A single named test scenario for the discovery conformance fixture.",
      "required": ["name", "description", "query", "expectedInstanceIds", "exactMatch"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Unique scenario identifier (snake_case, no spaces)."
        },
        "description": {
          "type": "string",
          "description": "Human-readable intent of the scenario."
        },
        "query": {
          "$ref": "#/$defs/DiscoveryQuery",
          "description": "The DiscoveryQuery to execute against the fixture repo."
        },
        "expectedInstanceIds": {
          "type": "array",
          "items": { "type": "string", "format": "uuid" },
          "description": "Minimum required result set. For exactMatch:true scenarios, this is also the maximum."
        },
        "exactMatch": {
          "type": "boolean",
          "description": "If true, the result set MUST equal expectedInstanceIds exactly (structured-filter scenario). If false, the result set MUST be a superset of expectedInstanceIds (content-match scenario)."
        },
        "expectedSegments": {
          "$ref": "#/$defs/ExpectedSegments",
          "description": "Optional. When present, the runner MUST additionally verify the exact ordered TextSegment sequence for one field of one instance — the segment COUNT and ORDER, which expectedInstanceIds/exactMatch alone cannot express (I-120's list-cardinality rule: one segment per array element, in array order)."
        }
      }
    },
    "ExpectedSegments": {
      "type": "object",
      "description": "A structured expectation over the exact ordered list of TextSegments one field of one instance projects. Added by RFC-012 [R11] (srs#483) to make segment count and order testable, closing the gap left by expectedInstanceIds alone.",
      "required": ["instanceId", "fieldName", "segments"],
      "additionalProperties": false,
      "properties": {
        "instanceId": {
          "type": "string",
          "format": "uuid",
          "description": "The instance whose Text Projection is inspected."
        },
        "fieldName": {
          "type": "string",
          "description": "The TextSegment.fieldName to filter the instance's projection to before checking count and order."
        },
        "segments": {
          "type": "array",
          "items": { "type": "string" },
          "description": "The exact ordered list of TextSegment.text values expected for fieldName. The runner MUST assert that the projected segment count equals segments.length and that segment i's text equals segments[i], for every i, in order."
        }
      }
    }
  }
}
