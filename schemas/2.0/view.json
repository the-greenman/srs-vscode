{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://srs.semanticops.com/schema/2.0/view.json",
  "title": "SRS View Definition (ext:views-l1)",
  "description": "A versioned presentation and export configuration over a field set. A View is compatible with any record containing its required fields.",
  "type": "object",
  "required": [
    "$schema",
    "id",
    "namespace",
    "name",
    "version",
    "description",
    "fieldViews",
    "createdAt"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "const": "https://srs.semanticops.com/schema/2.0/view.json"
    },
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Stable UUID for this View. Never changes across versions."
    },
    "namespace": {
      "type": "string",
      "description": "Logical namespace, e.g. 'com.semanticops.srs'."
    },
    "name": {
      "type": "string",
      "description": "Machine-readable name within the namespace, kebab-case."
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "description": {
      "type": "string",
      "description": "When to use this View; what workflow or audience it serves."
    },
    "compatibleTypes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional Type-key hints (namespace/name) this View was designed for. Informational only; not a compatibility constraint."
    },
    "fieldViews": {
      "type": "array",
      "items": {
        "oneOf": [
          {
            "$ref": "#/$defs/FieldView"
          },
          {
            "$ref": "#/$defs/RecordPropertyView"
          }
        ]
      },
      "description": "Presentation configuration for fields and record-level properties. Each item's order MUST be unique within this array; that cross-item constraint is enforced at package validation time because JSON Schema cannot express uniqueness by an object property. Field rows omitted here are treated as visible: false."
    },
    "exportConfig": {
      "$ref": "#/$defs/ExportConfig"
    },
    "aiGuidance": {
      "type": "object",
      "description": "Guidance for AI agents using this View.",
      "additionalProperties": false,
      "properties": {
        "purpose": {
          "type": "string"
        },
        "extraction": {
          "type": "string"
        }
      }
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "lineage": {
      "type": "object"
    },
    "provenance": {
      "type": "object"
    }
  },
  "$defs": {
    "FieldView": {
      "type": "object",
      "required": [
        "fieldId",
        "order"
      ],
      "additionalProperties": false,
      "properties": {
        "fieldId": {
          "type": "string",
          "format": "uuid",
          "description": "Must reference a valid Field.id in the effective package set."
        },
        "order": {
          "type": "integer",
          "minimum": 0,
          "description": "Display order within this View."
        },
        "required": {
          "type": "boolean",
          "description": "View-level workflow constraint; does not alter the Field contract."
        },
        "visible": {
          "type": "boolean",
          "description": "Default: true. A hidden field remains in the Record and may appear in other Views."
        },
        "displayLabel": {
          "type": "string"
        },
        "displayHint": {
          "type": "string"
        },
        "editorHintOverride": {
          "type": "string",
          "description": "Presentation only (Invariant 13, extended by RFC-036 [CR-036-20]). Supersedes Field.editorHint for Records rendered or edited through this View. RFC-036 [CR-036-21]: the value MUST come from the same set as Field.editorHint (singleline, textarea, rich-text, date-picker, dropdown, multi-select, voice); a value outside that set is ignored with a diagnostic and Field.editorHint applies. Deliberately not constrained to an enum here so an unrecognised value does not fail the load of the whole View."
        },
        "compositeRenderer": {
          "$ref": "#/$defs/CompositeRendererBinding",
          "description": "RFC-036 — render this field's composite-range value through a named composite renderer. Highest-precedence declaration site (RFC-036 [CR-036-6]). Valid only on a Field whose fieldType.datatype is 'ref' and mode is 'inline' ([CR-036-3])."
        }
      }
    },
    "RecordPropertyView": {
      "type": "object",
      "required": [
        "property",
        "order"
      ],
      "additionalProperties": false,
      "properties": {
        "property": {
          "type": "string",
          "enum": [
            "lifecycleState",
            "tags",
            "createdAt",
            "updatedAt"
          ],
          "description": "Record-level property presented by this row. The closed enum is generated from record.json by scripts/gen-record-property-view-enum.mjs."
        },
        "order": {
          "type": "integer",
          "minimum": 0,
          "description": "Display order within this View, on the same axis as FieldView.order."
        },
        "displayLabel": {
          "type": "string",
          "description": "Overrides the default label for this record-level property."
        },
        "visible": {
          "type": "boolean",
          "description": "Default: true. When false, this record-level property row is hidden."
        }
      }
    },
    "CompositeRendererBinding": {
      "type": "object",
      "required": [
        "renderer"
      ],
      "additionalProperties": false,
      "description": "RFC-036 Change A (ext:views-l1) — a view-owned composite rendering dispatch record. Presentation only: it MUST NOT affect validity, field values, Relations, or Discovery Text Projection ([CR-036-20]).",
      "properties": {
        "renderer": {
          "type": "string",
          "minLength": 1,
          "description": "Composite renderer identifier. Bare lower-kebab names are SRS-reserved and introduced only by a ratified RFC ('table', and the sentinel 'baseline' meaning explicitly no renderer); vendor identifiers use '{reverse-domain}/{name}' with at least two reverse-domain labels. The grammar ^([a-z][a-z0-9-]*|[a-z0-9-]+(\\.[a-z0-9-]+)+/[^/]+)$ is enforced at render and validation time rather than here ([CR-036-1]), so a malformed or unknown value falls back to the composite baseline with a diagnostic ([CR-036-7]) instead of failing the load."
        },
        "roles": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "format": "uuid"
          },
          "description": "Explicit, UUID-anchored role -> Field.id binding, overriding the by-name defaults of [CR-036-8]. Role names are defined by the renderer; the 'table' renderer defines rows and cells (required) plus columns, widths, subheading and label. A role the renderer does not define is silently ignored."
        }
      }
    },
    "ExportConfig": {
      "type": "object",
      "additionalProperties": false,
      "description": "Configuration for rendering a Record through this View as an exportable document.",
      "properties": {
        "format": {
          "type": "string",
          "description": "Target output format. Portable values: 'markdown', 'adoc', 'html', 'text'. Governs this View's own render context: standalone rendering when the View is used outside any Composition, and per-record rendering when a Composition section dispatches to this View (renderViewId/typeDispatch) -- but a dispatching Composition's document-level format comes from that Composition's own exportConfig.format, not from here (srs#525: one shape, two attachment points, no precedence between them)."
        },
        "preamble": {
          "type": "string",
          "description": "Template string rendered before field values. Standard variables: {{instance-id}}, {{date}}, {{status}}, {{namespace}}, {{name}}. In section rendering context, {{heading-3}} is also available (resolves to heading level 3 + depthOffset)."
        },
        "omitEmptyFields": {
          "type": "boolean",
          "description": "Default: false. When true, fields with no value are omitted from output."
        }
      }
    }
  }
}
