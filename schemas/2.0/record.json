{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://srs.semanticops.com/schema/2.0/record.json",
  "title": "SRS Record (Tier 2)",
  "description": "An instantiated Type with field values. A fully structured, settled instance. Intended for composition via allOf in domain schemas — $schema is not constrained here.",
  "type": "object",
  "required": [
    "instanceId",
    "typeId",
    "typeVersion",
    "typeNamespace",
    "typeName",
    "fieldValues"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "instanceId": {
      "type": "string",
      "format": "uuid"
    },
    "typeId": {
      "type": "string",
      "format": "uuid"
    },
    "typeVersion": {
      "type": "integer",
      "minimum": 1
    },
    "typeNamespace": {
      "type": "string"
    },
    "typeName": {
      "type": "string"
    },
    "lifecycleState": {
      "type": "string",
      "description": "ext:lifecycle — current lifecycle state of this Record."
    },
    "fieldValues": {
      "type": "object",
      "description": "RFC-039: the name-keyed recursive value carrier. Keys are Field.name verbatim ([R2b]); each value follows the recursive Change-B rule for that Field's fieldType — the instance space of RFC-032 projectField. null is not a value: absence of the key is the sole representation of unset ([R5]). The Type's projected JSON Schema validates this object's interior ([R17]); this envelope schema constrains only its shape.",
      "additionalProperties": {
        "not": {
          "type": "null"
        }
      }
    },
    "fieldMeta": {
      "type": "object",
      "description": "RFC-039 Change C: per-field provenance — metadata about the assertion, keyed identically to fieldValues. Keys MUST be a subset of the sibling fieldValues keys ([R6]). One object per field: never per list item, never inside an inline-composite interior.",
      "additionalProperties": {
        "$ref": "#/$defs/FieldMeta"
      }
    },
    "sourceRefs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/SourceReference"
      }
    },
    "tags": {
      "type": "array",
      "description": "Topic membership labels. When a Vocabulary is declared, each value MUST resolve to a Term key or alias (tier-graduated: Records enforce resolution; Notes do not).",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "meta": {
      "type": "object",
      "description": "Implementation-local key-value pairs. Use for lock state, visibility, session references. Cross-system keys should be namespaced (e.g. 'com.acme.locking.locked-by')."
    }
  },
  "$defs": {
    "FieldMeta": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "source": {
          "type": "string",
          "enum": [
            "human",
            "ai",
            "imported",
            "derived"
          ]
        },
        "editedAt": {
          "type": "string",
          "format": "date-time"
        },
        "sourceRefs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SourceReference"
          }
        }
      }
    },
    "SourceReference": {
      "type": "object",
      "required": [
        "sourceType",
        "sourceId"
      ],
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": { "sourceType": { "const": "repository-document" } },
            "required": ["sourceType"]
          },
          "then": {
            "properties": { "sourceId": { "format": "uuid" } }
          }
        }
      ],
      "properties": {
        "sourceType": {
          "type": "string",
          "enum": [
            "transcript-chunk",
            "transcript-segment",
            "external-document",
            "repository-document"
          ]
        },
        "sourceId": {
          "type": "string",
          "description": "Free-form when sourceType is transcript-chunk/transcript-segment/external-document. When sourceType is repository-document, MUST be a SourceDocument.documentId (format: uuid, RFC-017) — enforced by the sibling if/then."
        },
        "sourceStandard": {
          "type": "string"
        },
        "streamId": {
          "type": "string",
          "format": "uuid"
        },
        "sourceRole": {
          "type": "string",
          "enum": [
            "evidence",
            "extracted-from",
            "quoted-from",
            "inspired-by",
            "attaches"
          ],
          "description": "The role the source material plays for this instance (RFC-023). Replaces relationType. 'attaches' added by RFC-017: material attachment of a source document to a record (RFC-017 [R1])."
        },
        "confidence": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "note": {
          "type": "string"
        }
      }
    }
  }
}
