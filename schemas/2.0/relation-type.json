{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://srs.semanticops.com/schema/2.0/relation-type.json",
  "title": "SRS Relation Type Definition",
  "description": "Declares a named relation type within a package's relation type vocabulary. Relation types describe the semantic meaning and structural shape of a class of relations.",
  "type": "object",
  "required": [
    "$schema",
    "id",
    "version",
    "key",
    "namespace",
    "label",
    "description",
    "category",
    "createdAt"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Stable UUID identity for this relation type definition."
    },
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "Monotonically increasing version number. Starts at 1."
    },
    "key": {
      "type": "string",
      "description": "The string stored in Relation.relationType — a canonical bare string (e.g. `supersedes`) or custom `namespace/name` string (e.g. `org.example/my_type`). Unified key field per RFC-006 VocabularyEntry substrate. Must be globally unique across the effective installed package set."
    },
    "namespace": {
      "type": "string",
      "description": "The package namespace this relation type belongs to."
    },
    "label": {
      "type": "string",
      "description": "Short human-readable label for display in views and tooling."
    },
    "description": {
      "type": "string",
      "description": "Full description of the semantic meaning of this relation type."
    },
    "category": {
      "type": "string",
      "enum": [
        "composition",
        "refinement",
        "dependency",
        "sequence",
        "derivation",
        "evidence",
        "governance",
        "association",
        "lifecycle",
        "provenance",
        "other"
      ],
      "description": "Structural category."
    },
    "canonicalDirection": {
      "type": "string",
      "description": "Human-readable description of which end is source and which is target, or what members represent."
    },
    "inverseType": {
      "type": "string",
      "description": "Key of the derived inverse (e.g. supersedes -> superseded-by); need not resolve. Only the canonical forward form is ever stored or installed as a RelationTypeDefinition — inverse forms are derived display labels, never asserted (R2) — so this key is not guaranteed to name an installed RelationTypeDefinition."
    },
    "irreflexive": {
      "type": "boolean",
      "description": "When true, a relation from an instance to itself is invalid."
    },
    "requireSameType": {
      "type": "boolean",
      "description": "When true, source and target must resolve to the same bound Type. srs-rust#910: re-keys the retired requireSameSemanticObjectType onto the Type system (owner ruling on #383, srs#372/#524, rfc-decision-c8704763). allowedSourceTypes/allowedTargetTypes retired with no successor — both were keyed on the same retired semanticObjectType string and could never fire against a schema-conforming Record/Note."
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "deprecated",
        "tombstone",
        "retired"
      ],
      "description": "Lifecycle status. Absent means active. deprecated: resolves but new writes rejected. tombstone: resolves for reads only. retired: does not resolve."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when this definition was created."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when this definition was last updated."
    },
    "meta": {
      "type": "object",
      "additionalProperties": true,
      "description": "Arbitrary metadata per substrate Change H policy (rfc-decision-6fc7e142: the one escape-bag name, matching the instance layer's meta). Unknown top-level fields are rejected; use this instead."
    }
  },
  "additionalProperties": false
}
