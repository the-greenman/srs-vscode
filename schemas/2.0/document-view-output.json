{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://srs.semanticops.com/schema/2.0/document-view-output.json",
  "title": "SRS Document View JSON Projection Output (ext:views-l2)",
  "description": "Machine-readable projection produced by rendering a DocumentView with format 'json'. Contains document metadata, ordered sections, and typed record data. Not a rendered markup document.",
  "type": "object",
  "required": [
    "$schema",
    "compositionId",
    "containerId",
    "generatedAt",
    "sections"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "const": "https://srs.semanticops.com/schema/2.0/document-view-output.json"
    },
    "compositionId": {
      "type": "string",
      "format": "uuid",
      "description": "ID of the Composition that produced this projection. srs-rust#910: renamed from documentViewId — this file's own name/$id/shape stay parked pending #365's dedicated review (three disagreeing copies: this schema's unemitted relations member, the pre-#910 mirror's fieldGroups with no successor, and the real ProjectedRecord output matching neither)."
    },
    "containerId": {
      "type": [
        "string",
        "null"
      ],
      "description": "Container ID derived from the first ContainerSubset section source. Null when no ContainerSubset source is present in the view."
    },
    "generatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp of when this projection was generated."
    },
    "containerTitle": {
      "type": "string",
      "description": "Human-readable container title resolved at render time."
    },
    "preamble": {
      "type": "string",
      "description": "Evaluated DocumentView.preamble string. Heading variables ({{heading-N}}) are substituted as empty strings."
    },
    "sections": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ProjectedSection"
      },
      "description": "Ordered sections (by DocumentSection.order)."
    }
  },
  "$defs": {
    "ProjectedSection": {
      "type": "object",
      "required": [
        "sectionId",
        "order",
        "records"
      ],
      "additionalProperties": false,
      "properties": {
        "sectionId": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "order": {
          "type": "integer",
          "minimum": 0
        },
        "records": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ProjectedRecord"
          }
        }
      }
    },
    "ProjectedRecord": {
      "type": "object",
      "required": [
        "instanceId",
        "typeId",
        "typeVersion",
        "typeNamespace",
        "typeName",
        "fields",
        "orderedFieldKeys"
      ],
      "additionalProperties": false,
      "properties": {
        "instanceId": {
          "type": "string",
          "format": "uuid"
        },
        "typeId": {
          "type": "string",
          "format": "uuid"
        },
        "typeVersion": {
          "type": "integer",
          "minimum": 1,
          "description": "Version of the Type this record was projected from (Record.typeVersion, RFC-032/RFC-039) — the authoritative half of the record-to-type PINNED binding alongside typeId."
        },
        "typeNamespace": {
          "type": "string"
        },
        "typeName": {
          "type": "string"
        },
        "recordHeading": {
          "type": "string",
          "description": "Value of the titleFieldId field, if the section declares one and the record has it."
        },
        "preamble": {
          "type": "string",
          "description": "Evaluated export_config.preamble for this record. Heading variables substituted as empty strings. Present only when the section's render view has an ExportConfig.preamble."
        },
        "fields": {
          "type": "object",
          "description": "Typed field values keyed by Field.name (RFC-039 [R11]). Values are raw JSON (string, number, boolean, array, or object); a composite value is carried recursively under its own key, exactly as in the instance. Empty/absent fields are omitted when omitEmptyFields is true.",
          "additionalProperties": true
        },
        "orderedFieldKeys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Field.name keys in FieldAssignment.order — the iteration order for fields."
        },
        "relations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ProjectedRelationRow"
          },
          "description": "RFC-027 [I-027-8]. Present only when the section declares relationsPresentation and this record has surviving link rows; rows in display order. Omitted when no edges survive selection."
        },
        "properties": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ProjectedPropertyRow"
          },
          "description": "RFC-041 [R8]. Present only when the section's render view declares at least one RecordPropertyView row and this record has at least one surviving (non-omitted, [R4]) row; rows in ascending View.fieldViews[].order. Omitted when no property row survives selection."
        }
      }
    },
    "ProjectedPropertyRow": {
      "type": "object",
      "required": [
        "property",
        "label",
        "value"
      ],
      "additionalProperties": false,
      "properties": {
        "property": {
          "type": "string",
          "enum": [
            "lifecycleState",
            "tags",
            "createdAt",
            "updatedAt"
          ],
          "description": "RFC-041 Change B closed, generated enum — the RecordPropertyView.property this row presents."
        },
        "label": {
          "type": "string",
          "description": "Resolved row label per RFC-041 Change C: RecordPropertyView.displayLabel if set, else the per-property default (Status/Tags/Created/Updated)."
        },
        "value": {
          "description": "Resolved value per RFC-041 Change C: lifecycleState resolves through the bound Lifecycle's LifecycleState.label when [R6] applies, otherwise the raw state key; tags is the raw string array; createdAt/updatedAt are raw ISO-8601 strings.",
          "type": [
            "string",
            "array"
          ],
          "items": {
            "type": "string"
          }
        }
      }
    },
    "ProjectedRelationRow": {
      "type": "object",
      "required": [
        "relationType",
        "direction",
        "label",
        "targets"
      ],
      "additionalProperties": false,
      "properties": {
        "relationType": {
          "type": "string"
        },
        "direction": {
          "type": "string",
          "enum": [
            "forward",
            "inverse"
          ],
          "description": "Whether the record is the source (forward) or target (inverse) of the listed edges. Display-only; only forward edges are stored (Invariant 16)."
        },
        "label": {
          "type": "string",
          "description": "Resolved row label per RFC-027 Change B."
        },
        "targets": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/ProjectedRelationTarget"
          },
          "description": "Related instances in display order: display label ascending, instanceId ascending tiebreak, both by Unicode code point (RFC-027 [I-027-5])."
        }
      }
    },
    "ProjectedRelationTarget": {
      "type": "object",
      "required": [
        "instanceId",
        "displayLabel"
      ],
      "additionalProperties": false,
      "properties": {
        "instanceId": {
          "type": "string",
          "format": "uuid"
        },
        "displayLabel": {
          "type": "string",
          "description": "Resolved per RFC-027 Change C rule 5 (effective identityFieldId → section titleFieldId → instanceId)."
        }
      }
    }
  }
}
