{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ArchivePackPayload",
  "description": "Payload for `srs archive pack`. Output format is `.srs` (SRSzip, deterministic ZIP). Re-packing the same repository state produces byte-identical output. See ADR-033, ADR-036.",
  "type": "object",
  "required": [
    "fileSizeBytes",
    "outputPath"
  ],
  "properties": {
    "fileSizeBytes": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "outputPath": {
      "type": "string"
    }
  }
}
