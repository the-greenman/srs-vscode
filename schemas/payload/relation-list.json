{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RelationListPayload",
  "type": "object",
  "required": [
    "relations"
  ],
  "properties": {
    "relations": {
      "description": "Uses `RelationSummary` directly — its `serde(rename_all = \"camelCase\")` produces `{ \"relationId\", \"relationType\", \"sourceId\", \"targetId\" }`, plus `sourceLabel`/ `targetLabel` when the endpoint resolves to a display label (srs-rust#1046; omitted, not null, when it doesn't).",
      "type": "array",
      "items": true
    }
  }
}
