{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RelationTypeDeletePayload",
  "description": "Uses `id` (not `instanceId`) — relation-type definitions are package definitions, not instance-index members (ADR-009).",
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "diagnostics": {
      "description": "Notes, e.g., when the deleted definition's key remains resolvable through another installed definition or the embedded core package (srs-rust#995).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "id": {
      "type": "string"
    }
  }
}
