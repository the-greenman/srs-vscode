{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FindPayload",
  "description": "Payload for `find` — `ext:discovery` results: ordered hits, total, diagnostics.",
  "type": "object",
  "required": [
    "result"
  ],
  "properties": {
    "result": true
  }
}
