{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TypeListPayload",
  "type": "object",
  "required": [
    "types"
  ],
  "properties": {
    "types": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/TypeListEntry"
      }
    }
  },
  "definitions": {
    "TypeListEntry": {
      "description": "A single entry in a type list — the subset of `TypeSummary` exposed by the CLI. (Omits `description` which was never in the prior output. Carries `identityFieldId` and `lifecycleRef` so the discovery ladder's list step can tell whether a type has an identity field or a bound lifecycle without a `type get` per type; srs-rust#958.)",
      "type": "object",
      "required": [
        "fieldCount",
        "id",
        "name",
        "namespace",
        "version"
      ],
      "properties": {
        "fieldCount": {
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "id": {
          "type": "string"
        },
        "identityFieldId": {
          "type": [
            "string",
            "null"
          ]
        },
        "lifecycleRef": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": "string"
        },
        "namespace": {
          "type": "string"
        },
        "sourcePackage": {
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      }
    }
  }
}
