{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RecordDeletePayload",
  "description": "`record delete` (srs-rust#1025). Extends `DeletedPayload`'s shape with every relation the delete cascaded away — inbound or outbound — so a caller can see what provenance was removed instead of discovering a missing edge later. Empty when the deleted record had no incident relations.",
  "type": "object",
  "required": [
    "cascadedRelations",
    "instanceId"
  ],
  "properties": {
    "cascadedRelations": {
      "type": "array",
      "items": true
    },
    "instanceId": {
      "type": "string"
    }
  }
}
