{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RecordGetPayload",
  "description": "Payload for `record get` — the full `Record` plus its core-resolved `displayLabel`. Shape mirrors `RecordSummary` from srs-repository.",
  "type": "object",
  "required": [
    "displayLabel",
    "instanceId",
    "record"
  ],
  "properties": {
    "displayLabel": {
      "type": "string"
    },
    "instanceId": {
      "type": "string"
    },
    "record": true
  }
}
