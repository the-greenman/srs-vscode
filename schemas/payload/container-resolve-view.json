{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ContainerViewPayload",
  "description": "Payload for `container resolve-view` (issue #254, #256).\n\nCarries the structured container view: the container root record, ordered member records (Tier-0, Tier-1, or Tier-2; full `Record` present only for Tier-2), the Composition-driven column spec, and non-fatal diagnostics.",
  "type": "object",
  "required": [
    "containerView"
  ],
  "properties": {
    "containerView": true
  }
}
